export interface IDriveModalProps {
  onCloseModal: () => void;
}

export interface IDriveModalInitialValues {
  name: string;
  phoneNumber: string;
  email: string;
  preferredDate: string;
  preferredTime: string;
}
